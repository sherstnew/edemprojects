import { Layout } from '../components/Layout/Layout';
import { CreateProject } from '../blocks/CreateProject/CreateProject';

export function CreateProjectPage () {
  return (
    <Layout>
      <CreateProject />
    </Layout>
  );
}
